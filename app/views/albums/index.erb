<h1><%=current_user.username%>'s RecordCo//ection</h1>
<div class="sidebar">
<ul>
  <% current_user.albums.each do |album| %>
    <li><a href="/albums/<%=album.id%>"><%="#{album.name} - by #{album.artist.name}"%></a></li>
  <% end %>
</ul>
</div>

<br>
<br>

<button class="btn-primary" onclick="location.href='/albums/new'">Add an Album to your Collection</button>